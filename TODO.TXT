-- Pensar em uma forma de os artefatos criados pertecerem ao usuário "firebird" (ao menos ao final do projeto quando ele será instalado na pasta do Firebird);
-- O passo acima deve contemplar tantos arquivos de planejamento, quanto de banco de dados;
-- Implementar o tratamento de linha de comando (tratamento para que, caso o comando não esteja completo, não execute a ação e lance um erro);
-- Criar um "README.md" detalhado, com exemplos de utilização (em PT-BR);
-- Criar uma arquivo "help.txt" seguindo a idéia de uma aquivo de ajuda "padrão Linux CLI" - criar em /util/help.txt (pense em criar uma classe Help.h/cpp);
-- Criar check-list com necessidade de istalação (server-side e client-side) - lembra tanto dos requisitos de SO, BD e C++;
-- O README deve ser o manual para utilização do programa (incluir imagens, componentes e etc)


-- Na estrutura base do projeto, não aparece os arquivos /include/DBManager.h e /src/DBManager.cpp
-- ATENCAO: LEMBRA DE APAGAR O ARQUIVO REPLDB.FDB (DEVE SER GERADO AUTOMATICAMENTE PELO PROGRAMA)
-- ATENCAO: QUESTIONAR SE O INIT.SQL NÃO É REDUNDANTE JÁ QUE A ESTRUTURA DO BANCO ESTÁ SENDO CRIADA DENTRO DO ARQUIVO CPPS
-- ATENCAO: no caso de estar utilizando VMs, lembrar de corrigir o date time: sudo timedatectl set-timezone America/Sao_Paulo
-- Se o backup fosse feito na mão:
    01 sudo /opt/firebird/bin/gbak -b /opt/firebird/data/players.fdb /opt/firebird/data/players.fbk
    02 sudo chown firebird:firebird /opt/firebird//data/players.fbk
    03 sudo /opt/firebird/bin/gbak -rep /opt/firebird/data/players.fbk 192.168.1.54:/opt/firebird/data/players-replica01.fdb -user sysdba -pass repl@gis123

-- Se o backup for feito de um lado e restaurado no outro
    01 sudo /opt/firebird/bin/gbak -b -se 192.168.1.53:service_mgr -g -user sysdba -pass repl@gis123 /opt/firebird/data/players.fdb stdout | /opt/firebird/bin/gbak -rep -se 192.168.1.54:service_mgr -user sysdba -pass repl@gis123 stdin /opt/firebird/data/players-replica01.fdb
